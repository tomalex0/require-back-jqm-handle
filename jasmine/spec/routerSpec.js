define(['router', 'jquery'], function(AppRouter, $) {
	/*describe("Router", function() {
	  afterEach( function(){
	    Backbone.history.stop();
	     //$('div[data-role="page"]').remove();
	    //  $('.ui-loader').remove();
	  });
	
	  it("should call defaultAction", function(){
	    spyOn(AppRouter.prototype, "defaultAction");
	    var router = new AppRouter(); // instance created after spy activation
	    Backbone.history.start();      // it has to start after the Router instance is created
	
	    router.navigate('', true);
	
	    expect(AppRouter.prototype.defaultAction).toHaveBeenCalled();  
	  });
	});*/
	
});